import React from 'react';
import PropTypes from 'prop-types';
import {
  Text,
} from '@bufferapp/components';

const AppSidebar = ({
  translations
}) =>
  <Text>This is the app AppSidebar!</Text>;

AppSidebar.propTypes = {
};

AppSidebar.defaultProps = {
};

export default AppSidebar;
